this folder contains exactly one command line interface; the interface may support http access in the future; the main interactions though will be a user watching the dashboard and issuing key bindings; the program should run with minimal footprint; it's mostly going to be text based; the program will eventually use modern TUI elements like BubbleTea or such; this binary is written in golang because the alarm management software it will interact with first is golang; the binary might be tasked with executing arbitratry binary commands on the host; i.e. ansible playbooks (which trigger changes on other machines); the cli should support otel; one core feature of the binary will be 'lights', a series of indicators or lights that are red/green based on service or feature; to give the system something to start with the tool should launch the fire-marshal and it's tests; the initial lights should be based off existing gherkin features; the cli should have it;s own gherkin features; if the tool has any skills available they should be clear on how to work with the tool
